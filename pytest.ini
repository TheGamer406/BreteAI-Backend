[pytest]
markers =
    contract: pega a APIs externas reales (docs/design.md §4). No corre en CI normal.

# Por defecto excluye los tests de contrato -- correrlos aparte con:
#   pytest -m contract
addopts = -m "not contract"
